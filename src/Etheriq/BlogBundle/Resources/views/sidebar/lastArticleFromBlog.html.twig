<div class="left-sidebar">
    <ul>
        {% for item in blogs %}
            <li><a href="{{ path('blog_showInfo', {'slug': item.slug}) }}">{{ item.title }}</a></li>
        {% endfor %}
    </ul>
</div>
